{
  "name": "casa-jb-agendamento",
  "version": "1.0.0",
  "description": "Sistema de agendamento de salas — Casa JB",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "node --experimental-sqlite --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
